name: podium
description: "Podium — suivi de scores et classements entre amis."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.5.0
  provider: ^6.1.2
  google_fonts: ^6.2.1
  intl: ^0.20.2
  flutter_localizations:
    sdk: flutter
  uuid: ^4.5.1
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.2.0
  fl_chart: ^1.2.0
  shared_preferences: ^2.5.5
  firebase_messaging: ^15.2.10
  flutter_local_notifications: ^22.0.1
  connectivity_plus: ^6.1.0
  pdf: ^3.13.0
  printing: ^5.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
